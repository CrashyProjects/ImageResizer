# Default directory path, will be used only if not passed as argument
directory = "."  

# Desired destination path, if set to "" will be the same has "directory"
destination = ""

# If True, a copy of the resized image will be saved as "RESIZED_filename.ext", if False, the original image will be overwritten
saveCopy = True  

# Desired resize dimensions in pixels (width, height)
resizeRes = (1080, 1920)